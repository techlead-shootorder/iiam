import MobileNavPage from "@/app/_shared/MobileNavPage";

export default function Page() {
  return (
    <MobileNavPage
      pageIndex={5}
      title="Discover IAAM"
      slug="discover-iaam"
    />
  );
}
